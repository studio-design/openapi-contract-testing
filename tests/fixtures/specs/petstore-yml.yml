openapi: 3.0.3
info:
  title: Petstore YML
  version: 1.0.0
paths:
  /v1/pets:
    get:
      summary: List all pets
      operationId: listPets
      responses:
        '200':
          description: A list of pets
          content:
            application/json:
              schema:
                type: object
